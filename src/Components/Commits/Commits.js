import React from 'react';
import styles from './Commits.modules.scss';

const Commits = (props) => {
    console.log(props.commits);
    return(
        <section className={styles.Commit}>

        </section>
    );
};

export default Commits;