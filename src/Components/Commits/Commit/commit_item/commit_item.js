import React from 'react';
import styles from './commit.module.scss';

const commit_item = (props) => {
    return(
        <div className={styles.Commit_Item}>

        </div>
    );
};

export default commit_item;